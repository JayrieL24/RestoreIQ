# Circular logo favicon

Source: public/RestoreIQ-Logo.png (retained unchanged).
Tool: built-in image generation, edit mode.
Generated asset: public/RestoreIQ-Logo-circle.png.
Installed favicon: public/favicon-circle.svg, embedding the generated image with a circular clipping mask to exclude the generated checkerboard outside the white disk.

Prompt: Add ONLY a flat pure-white circular frame/background behind the complete existing logo. Square PNG favicon asset, white circle centered and nearly touching the four canvas edges, genuine transparent alpha outside circle. Keep the original entire logo unchanged: same house and water drop, four windows, navy/blue gradients, shape, text 'RestoreIQ' and 'WATER DAMAGE' and two blue rules, identical relative proportions, no redesign or missing text. Center the complete logo comfortably within the circle, with enough white margin to avoid any clipping. No border color, no shadow, no bevel, no other elements. Preserve source identity and details; only convert rectangular white background to circular white background with transparent outer corners.

The generated design was visually inspected; generative editing cannot guarantee exact pixel identity. Its outer checkerboard is excluded by the favicon's circular SVG clip.
